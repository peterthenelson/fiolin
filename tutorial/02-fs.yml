meta:
  title: 2. The Filesystem
  author: Peter Nelson
  description: >
    /input: Files selected by the user are mounted here.

    /output: Files you write here are downloaded when the script ends.

    /tmp: Write whatever you want here; they'll disappear when the run is over.

    /home: The default current directory.
interface:
  inputFiles: SINGLE
  outputFiles: MULTI
runtime: {}