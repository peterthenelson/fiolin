meta:
  title: 2c. Files -- The rest of the filesystem.
  author: Peter Nelson
  description: >
    /tmp: Write whatever you want here; the files will disappear when the run is over.

    /home: The default current directory.
interface:
  inputFiles: SINGLE
  outputFiles: ANY
runtime: {}