meta:
  title: 3a. Basic UI -- Multiple inputs (or none!)
  author: Peter Nelson
  description: >
    The scripts we've written so far all take a single input file, but we can
    configure fiolin to accept multiple inputs (or none at all).
interface:
  inputFiles: SINGLE
  outputFiles: NONE
runtime: {}